# Specify which service to subscribe to.
- service: eat-jenkins          ## Name of the service subscription
  teamKey: jicv            # EAT Jenkins Team Key for the given Jenkins instance (ie. TABC)
  jenkinsType: dev
  environment: Production