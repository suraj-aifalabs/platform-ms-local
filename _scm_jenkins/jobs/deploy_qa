folder('platform-ms')
pipelineJob('platform-ms/deploy-qa') {
    definition {
        cpsScm {
          scm {
            git {
                branch('release')
                remote {
                    url('https://sourcecode.jnj.com/scm/asx-jicv/platform-ms.git')
                    credentials('sourcecode-bitbucket')
                }
            }
          }
        }
    }
}