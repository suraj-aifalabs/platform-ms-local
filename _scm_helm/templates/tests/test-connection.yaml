apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atara.fullname" . }}-test-connection"
  labels:
    {{- include "atara.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atara.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
